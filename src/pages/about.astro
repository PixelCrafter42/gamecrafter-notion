---
import { ContentPage } from '../theme';
import { site } from '../site.config';
---
<ContentPage eyebrow={site.pages.about.eyebrow} heading={site.pages.about.title} intro={site.pages.about.description}>
  <Fragment set:html={site.pages.about.html} />
</ContentPage>
